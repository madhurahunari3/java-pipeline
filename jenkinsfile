pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
              bat 'javac hello.java'
            }
        }

   stage('run') {
            steps {
              bat 'javac hello.java'
            }
        }
    }
}

